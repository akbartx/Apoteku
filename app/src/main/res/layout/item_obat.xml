<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="8dp">

    <ImageView
        android:id="@+id/imageViewObat"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:srcCompat="@tools:sample/avatars" />

    <TextView
        android:id="@+id/textViewNamaObat"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:textSize="18sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/textViewDeskripsiObat"
        app:layout_constraintStart_toEndOf="@id/imageViewObat"
        app:layout_constraintEnd_toEndOf="parent"
        android:textColor="@color/black"
        tools:text="Nama Obat" />

    <TextView
        android:id="@+id/textViewDeskripsiObat"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@+id/textViewNamaObat"
        app:layout_constraintBottom_toTopOf="@+id/textViewHargaObat"
        app:layout_constraintStart_toEndOf="@id/imageViewObat"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="Deskripsi Obat" />

    <TextView
        android:id="@+id/textViewHargaObat"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginBottom="8dp"
        android:textSize="16sp"
        app:layout_constraintTop_toBottomOf="@+id/textViewDeskripsiObat"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/imageViewObat"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="Rp.1000" />

</androidx.constraintlayout.widget.ConstraintLayout>
